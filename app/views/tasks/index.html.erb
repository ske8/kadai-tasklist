<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |tasuk| %>
    <li><%= link_to tasuk.id, tasuk %> : <%= tasuk.content %> &gt; <%= tasuk.content %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>